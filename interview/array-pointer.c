#include <stdio.h>


int add_numbers(int *numbers_pointer) {
    // int sum = 0;
    // size_t size = sizeof numbers / sizeof numbers[0];

    // for (int i = 0; i < size; i++) {
    //     sum += numbers[i];
    // }

    // return sum;
    return numbers;
}

void main(){
    int my_numbers[3] = {1, 2, 3};
    printf("%d", add_numbers(my_numbers));
}
